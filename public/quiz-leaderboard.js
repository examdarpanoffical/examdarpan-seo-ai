(function(){
  if(location.pathname !== '/quiz') return;
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
  const tierFor=(score,total)=>{const pct=total?score/total*100:0;if(pct>=100)return{name:'LEGEND',icon:'👑',class:'legend'};if(pct>=90)return{name:'CHAMPION',icon:'🏆',class:'champion'};if(pct>=80)return{name:'PRO',icon:'💎',class:'pro'};if(pct>=70)return{name:'RISING STAR',icon:'🔥',class:'rising'};if(pct>=50)return{name:'CONTENDER',icon:'⚡',class:'contender'};return{name:'ROOKIE',icon:'🚀',class:'rookie'}};
  const maskName=name=>{const clean=String(name||'Student').trim().replace(/\s+/g,' ');if(!clean)return'Student';const p=clean.split(' ');if(p.length===1)return p[0].length>12?p[0].slice(0,11)+'…':p[0];return`${p[0]} ${p[p.length-1].charAt(0).toUpperCase()}.`};
  const css=`.ed-leaderboard{margin:0 20px 28px;padding:22px;border-radius:22px;background:linear-gradient(145deg,#fff,#f8fbff);border:1px solid #dbeafe;box-shadow:0 14px 40px rgba(15,23,42,.08)}.ed-lb-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:16px}.ed-lb-eyebrow{font-size:11px;letter-spacing:1.2px;font-weight:900;color:#2563eb}.ed-lb-title{margin:4px 0 0;color:#0f172a;font-size:24px;line-height:1.2}.ed-lb-live{padding:7px 11px;border-radius:999px;background:#ecfdf5;color:#047857;font-size:11px;font-weight:900;white-space:nowrap}.ed-lb-self{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:12px;padding:14px;border-radius:17px;background:#eff6ff;border:1px solid #bfdbfe;margin-bottom:16px}.ed-lb-rank{font-weight:950;color:#1d4ed8;font-size:20px;min-width:34px;text-align:center}.ed-lb-self strong{display:block;color:#0f172a;font-size:15px}.ed-lb-self small{display:block;margin-top:3px;color:#64748b}.ed-lb-badge{padding:7px 9px;border-radius:10px;font-size:11px;font-weight:900;white-space:nowrap}.ed-lb-badge.legend,.ed-lb-badge.champion{background:#fef3c7;color:#92400e}.ed-lb-badge.pro{background:#ede9fe;color:#6d28d9}.ed-lb-badge.rising{background:#fff7ed;color:#c2410c}.ed-lb-badge.contender{background:#ecfeff;color:#0e7490}.ed-lb-badge.rookie{background:#f1f5f9;color:#475569}.ed-lb-section-title{display:flex;align-items:center;justify-content:space-between;margin:4px 0 10px;color:#0f172a;font-weight:900}.ed-lb-section-title span{font-size:12px;color:#64748b;font-weight:700}.ed-lb-list{display:grid;gap:8px}.ed-lb-row{display:grid;grid-template-columns:38px 1fr auto auto;align-items:center;gap:10px;padding:11px 12px;border:1px solid #e2e8f0;border-radius:14px;background:#fff}.ed-lb-row.me{border-color:#93c5fd;background:#f8fbff;box-shadow:0 5px 16px rgba(37,99,235,.07)}.ed-lb-pos{font-weight:950;color:#64748b;text-align:center}.ed-lb-name{font-weight:850;color:#0f172a;font-size:14px}.ed-lb-score{font-weight:950;color:#1d4ed8}.ed-lb-mini{font-size:10px;color:#64748b}.ed-lb-next{margin-top:14px;padding:12px 14px;border-radius:14px;background:#f8fafc;color:#475569;font-size:13px;line-height:1.5}.ed-lb-next b{color:#0f172a}@media(max-width:640px){.ed-leaderboard{margin:0 12px 22px;padding:16px}.ed-lb-title{font-size:20px}.ed-lb-row{grid-template-columns:32px 1fr auto}.ed-lb-row .ed-lb-badge{display:none}.ed-lb-score{font-size:13px}.ed-lb-self{grid-template-columns:auto 1fr}.ed-lb-self .ed-lb-badge{grid-column:2;justify-self:start}}`;
  const injectStyle=()=>{if(document.getElementById('ed-leaderboard-style'))return;const s=document.createElement('style');s.id='ed-leaderboard-style';s.textContent=css;document.head.appendChild(s)};
  async function init(){try{
    const {getApp,getApps}=await import('https://www.gstatic.com/firebasejs/12.5.0/firebase-app.js');const app=getApps().length?getApp():null;if(!app)return;
    const {getAuth}=await import('https://www.gstatic.com/firebasejs/12.5.0/firebase-auth.js');
    const {getFirestore,collection,doc,setDoc,getDocs,query,where,limit}=await import('https://www.gstatic.com/firebasejs/12.5.0/firebase-firestore.js');
    const auth=getAuth(app),db=getFirestore(app);
    const waitForAuth=()=>new Promise(resolve=>{if(auth.currentUser)return resolve(auth.currentUser);let done=false;const timer=setInterval(()=>{if(auth.currentUser&&!done){done=true;clearInterval(timer);resolve(auth.currentUser)}},150);setTimeout(()=>{if(!done){done=true;clearInterval(timer);resolve(null)}},10000)});
    const getQuizId=async()=>{const urlId=new URLSearchParams(location.search).get('id');if(urlId)return urlId;const snap=await getDocs(query(collection(db,'quizzes'),where('status','==','published'),where('quizDate','==',today()),limit(1)));return snap.docs[0]?.id||null};
    const loadRows=async(quizId)=>{if(!quizId)return[];const snap=await getDocs(query(collection(db,'quizLeaderboard'),where('quizId','==',quizId),limit(100)));return snap.docs.map(d=>({id:d.id,...d.data()})).filter(x=>Number.isFinite(Number(x.score))).sort((a,b)=>Number(b.score)-Number(a.score)||(Number(a.submittedAt||0)-Number(b.submittedAt||0)))};
    const publish=async()=>{const result=document.getElementById('quizResult');if(!result||result.hidden)return;const ring=result.querySelector('.score-ring strong');if(!ring)return;const m=ring.textContent.trim().match(/(\d+)\s*\/\s*(\d+)/);if(!m)return;const score=Number(m[1]),total=Number(m[2]);const signature=`${score}/${total}|${result.querySelector('.result-hero h1')?.textContent||''}`;if(window.__edLbLastSignature===signature&&window.__edLbVisible)return;window.__edLbLastSignature=signature;window.__edLbVisible=true;
      const user=await waitForAuth();if(!user)return;const quizId=await getQuizId();if(!quizId)return;const studentName=localStorage.getItem('examDarpanStudentName')||'Student';const cleanName=maskName(studentName);const tier=tierFor(score,total);const entryId=`${quizId}_${user.uid}`.replace(/[^a-zA-Z0-9_-]/g,'_');await setDoc(doc(db,'quizLeaderboard',entryId),{quizId,studentUid:user.uid,studentName:cleanName,score,total,tier:tier.name,submittedAt:Date.now(),updatedAt:Date.now()},{merge:true});
      const rows=await loadRows(quizId);const rank=Math.max(1,rows.findIndex(x=>x.studentUid===user.uid)+1);const top=rows.slice(0,5);const myTier=tierFor(score,total);const nextBetter=rows.find(x=>Number(x.score)>score);const points=nextBetter?Number(nextBetter.score)-score:0;injectStyle();
      const card=document.createElement('section');card.className='ed-leaderboard';card.innerHTML=`<div class="ed-lb-head"><div><div class="ed-lb-eyebrow">🏆 TODAY'S TOPPER LEAGUE</div><h2 class="ed-lb-title">आज की Ranking</h2></div><span class="ed-lb-live">● LIVE</span></div><div class="ed-lb-self"><div class="ed-lb-rank">#${rank}</div><div><strong>${esc(cleanName)}</strong><small>${score}/${total} · ${Math.round(score/Math.max(total,1)*100)}% · आज की आपकी position</small></div><span class="ed-lb-badge ${myTier.class}">${myTier.icon} ${myTier.name}</span></div><div class="ed-lb-section-title">Top Performers <span>${rows.length} students</span></div><div class="ed-lb-list">${top.length?top.map((r,i)=>{const t=tierFor(Number(r.score),Number(r.total)||total);return `<div class="ed-lb-row ${r.studentUid===user.uid?'me':''}"><div class="ed-lb-pos">${i===0?'🥇':i===1?'🥈':i===2?'🥉':'#'+(i+1)}</div><div class="ed-lb-name">${esc(r.studentName||'Student')}${r.studentUid===user.uid?' <span class="ed-lb-mini">YOU</span>':''}</div><div class="ed-lb-score">${Number(r.score)}/${Number(r.total)||total}</div><span class="ed-lb-badge ${t.class}">${t.icon} ${t.name}</span></div>`}).join(''):'<div class="ed-lb-next">आज अभी ranking बन रही है. पहला test देकर leaderboard में अपनी जगह बनाइए.</div>'}</div><div class="ed-lb-next">${nextBetter?`🎯 <b>${points} more correct answer${points>1?'s':''}</b> से आप अगली position को challenge कर सकते हैं।`:`👑 <b>आप अभी सबसे ऊपर हैं!</b> आज का #1 बचाकर रखिए।`}</div>`;const target=result.querySelector('.result-actions');if(target)target.insertAdjacentElement('afterend',card);else result.appendChild(card);
    };
    const observe=()=>{const result=document.getElementById('quizResult'),appEl=document.getElementById('quizApp');if(!result)return;const mo=new MutationObserver(()=>{if(appEl&&!appEl.hidden){window.__edLbVisible=false;return}if(!result.hidden)setTimeout(publish,350)});mo.observe(result,{childList:true,subtree:true});if(!result.hidden)setTimeout(publish,500)};
    if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',observe,{once:true});else observe();
  }catch(e){console.warn('[ExamDarpan leaderboard]',e)}}
  init();
})();
